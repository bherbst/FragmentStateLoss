<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/content_second"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.example.stateloss.SecondActivity"
    tools:showIn="@layout/activity_second">

    <TextView
        android:text="Close me to see if we lost state!"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/textView"
        app:layout_constraintBottom_toBottomOf="@+id/content_second"
        app:layout_constraintRight_toRightOf="@+id/content_second"
        app:layout_constraintLeft_toLeftOf="@+id/content_second"
        app:layout_constraintTop_toTopOf="@+id/content_second"/>
</android.support.constraint.ConstraintLayout>
